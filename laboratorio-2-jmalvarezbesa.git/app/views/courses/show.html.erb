<p id="notice"><%= notice %></p>

<p>
  <strong>Curso:</strong>
  <%= @course.title %>
</p>

<p>
  <strong>Código:</strong>
  <%= @course.code %>
</p>

<p>
  <strong>Profesor:</strong>
  <%= @course.Person.first_name %> <%= @course.Person.last_name %>
</p>

<p>
  <strong>Alumnos:</strong>
  <table>
    <thead>
      <tr>
        <th>Nro</th>
        <th>Alumno</th>
        <th>Email</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @student_courses.each do |student_course| %>
      <tr>
        <td><%= student_course.Person.id %></td>
        <td><%= student_course.Person.first_name %> <%= student_course.Person.last_name %></td>
        <td><%= student_course.Person.email %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</p>

<form>
  <p>
  <strong>Agregar Alumno:</strong><br />
  <select name="category[id]" <%= collection_select(:id, 1, Person.where.not(:id => @student_courses), :id, :email) %></select>
  <button type="summit">Agregar</button>
</p>
</form>

<%= link_to 'Editar', edit_course_path(@course) %> |
<%= link_to 'Volver', courses_path %>
